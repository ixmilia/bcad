<ToolBar x:Class="BCad.UI.LayerToolBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ToolBar.Resources>
        <DataTemplate x:Key="LayerTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="TODO: VIS" />
                <Rectangle Grid.Column="1" Margin="1" Width="15" Height="15">
                    <Rectangle.Fill>
                        <SolidColorBrush Color="{Binding Path=Color.MediaColor}" />
                    </Rectangle.Fill>
                </Rectangle>
                <TextBlock Grid.Column="2" Text="{Binding Name}" />
            </Grid>
        </DataTemplate>
    </ToolBar.Resources>
    <ComboBox Name="currentLayer"
                Width="250"
                SelectionChanged="currentLayer_SelectionChanged"
                ItemTemplate="{StaticResource LayerTemplate}" />
    <Button Click="Layers_Click">Layers</Button>
</ToolBar>
