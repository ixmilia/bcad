using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace BCad.Extensions
{
    public static class ShapeExtensions
    {
        //public static Point Center(this Ellipse circle)
        //{
        //    var topLeft = new Point(Canvas.GetLeft(circle), Canvas.GetTop(circle), 0.0);
        //    return new Point(topLeft.X + circle.ActualWidth / 2.0, topLeft.Y + circle.ActualHeight / 2.0, 0.0);
        //}

        //public static double Radius(this Ellipse circle)
        //{
        //    return circle.ActualHeight / 2.0;
        //}
    }
}
