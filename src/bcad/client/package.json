{
  "name": "bcad-client",
  "version": "42.42.42",
  "description": "Self-contained HTML client for BCad.",
  "repository": "https://github.com/ixmilia/bcad",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "compile:webpack": "npx webpack-cli out/main.js -o out/bcad-client.js --mode development",
    "compile": "npm run build && npm run compile:webpack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "IxMilia",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^3.9.7",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.12"
  },
  "dependencies": {
    "resize-observer": "^1.0.0"
  }
}
